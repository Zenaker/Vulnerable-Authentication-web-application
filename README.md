# Vulnerable-Authentication-web-application
a vulnerable authentication web application

# Set up
set your email credentials on the `config.ini` file

# Start web server
start the web server with `python3 index.py`
